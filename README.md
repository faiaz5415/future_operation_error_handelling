## ⏳ future operation error handelling

```markdown
# ⏳ Async User Data Fetcher

This Dart project demonstrates how to simulate an asynchronous data request and handle custom exceptions using `Future`, `await`, and `try-catch`.

## ✨ Features
- Simulates a delayed network response using `Future.delayed`
- Returns data if `userId < 100`
- Throws `CustomException` for invalid user ID
- Handles errors using `try-catch` block

## 📦 Technologies
- Dart
- Async programming (`Future`, `await`)
- Custom exceptions
- Error handling

## ▶️ How to Run
```bash
dart async_user_fetch.dart
